## Development-first setup

This package has been cleaned for local development. Use `npm run install:all`, then `npm run dev`. Run `npm run verify` before pushing. A minimal CI workflow is kept, while Docker/CD deployment extras were removed from this copy.

# ticket-bro

## Docker Deployment

A production-style Docker deployment setup is included. See `DEPLOYMENT.md` for full instructions.
